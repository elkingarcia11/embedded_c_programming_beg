/* # is preprocessor directive that instructs the preprocessor
 * of the compiler to include a file into source file
 * */
#include <stdio.h>


int main() {
	/* printf is written in C's standard library
	compiler needs to understand prototype
	which is written in header file stdio.h*/
	printf("Hello World !");
}
